version: 2
updates:
  # Action majors move independently and fast; a pinned SHA is only safe if
  # something keeps it current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
